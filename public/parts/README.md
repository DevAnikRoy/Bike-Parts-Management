# Part thumbnails

Real photos for each part **type** (Hero vs Bajaj share the same image).

- Prefer `*.jpg` / `*.png` (see `ATTRIBUTION.md`)
- SVG files are fallback icons only
- Re-fetch: `node scripts/fetch-real-part-photos.mjs` or `node scripts/fix-part-photos.mjs`
