# Optional: when set, app can connect to Supabase (cloud sync for future mobile apps).
# Without these, the app runs fully in the browser (localStorage) — good for single-shop Netlify demo.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
