# Local: copy this file to .env and fill both values.
# Netlify: set the same two names under Site configuration → Environment variables, then redeploy.
# Vite bakes these in at build time. Do not commit .env.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
